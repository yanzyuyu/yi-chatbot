import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yanzy.intelligence',
  appName: 'Yanzy Intelligence',
  webDir: 'dist'
};

export default config;
